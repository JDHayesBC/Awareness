# Pattern Persistence System - Environment Configuration
# Copy this file to .env and customize for your setup

# === REQUIRED CONFIGURATION ===

# Project root directory (used for Docker bind-mounts)
# This should be the absolute path to your Awareness repository clone
# Used by docker-compose to mount chromadb_data and neo4j_data
PROJECT_ROOT=/path/to/Awareness

# Entity directory path (where identity, memories, and data live)
# This should point to your entity's folder (e.g., entities/my_entity)
# All entity-specific data (identity files, word-photos, databases) lives here
ENTITY_PATH=/path/to/Awareness/entities/my_entity

# Claude home directory (legacy shared data path)
# Used for logs and state files. Most data now lives in ENTITY_PATH.
# Typical location: /home/user/.claude
CLAUDE_HOME=/home/user/.claude

# === NETWORK CONFIGURATION ===

# Port mappings (change if these ports are already in use)
CHROMA_PORT=8200    # ChromaDB vector database
PPS_PORT=8201       # PPS HTTP API server
WEB_PORT=8202       # PPS Web Dashboard
GRAPHITI_PORT=8203  # Graphiti API server

# === OPTIONAL CONFIGURATION ===

# Timezone (for timestamps and logging)
TZ=America/Los_Angeles

# Docker Compose project name (helps avoid conflicts)
COMPOSE_PROJECT_NAME=pps

# === API KEYS ===

# OpenAI API key (REQUIRED for Graphiti entity extraction)
OPENAI_API_KEY=

# Embedding API keys (optional - uses local sentence-transformers if not set)
JINA_API_KEY=

# === DATABASE CONFIGURATION ===

# Neo4j password for graph database (used by Graphiti)
NEO4J_PASSWORD=password123

# Message database path (inside container, relative to ENTITY_PATH mount)
# Default: /app/entity/data/conversations.db
# PPS_MESSAGE_DB_PATH=/app/entity/data/conversations.db

# Graphiti group_id for entity isolation in shared graph database
# Set to your entity name to namespace graph data (e.g., "lyra", "caia")
# If not set, derived from ENTITY_PATH directory name
# GRAPHITI_GROUP_ID=
