# Additional dependencies for Docker deployment

# ChromaDB client for vector storage
chromadb>=0.4.0

# HTTP server for Docker (MCP over HTTP instead of stdio)
fastapi>=0.100.0
uvicorn>=0.23.0

# Embedding models
sentence-transformers>=2.2.0  # Local embeddings fallback
openai>=1.0.0                 # OpenAI embeddings option
httpx>=0.25.0                 # For JINA API calls

# Layer 3 (Graphiti) integration
aiohttp>=3.8.0                # Async HTTP client for Graphiti API (fallback mode)
graphiti-core>=0.5.0          # Direct Graphiti integration with custom entity types
pydantic>=2.0.0               # For custom entity type definitions
