# Pattern Persistence System - Dependencies

# MCP SDK for Claude Code integration
mcp>=1.0.0

# Layer 3 (Graphiti) integration
aiohttp>=3.8.0         # Async HTTP client for Graphiti API (fallback mode)
graphiti-core>=0.5.0   # Direct Graphiti integration with custom entity types
pydantic>=2.0.0        # For custom entity type definitions

# Vector store for Layer 2 and Tech RAG
chromadb>=0.4.0

# Future dependencies (uncomment as layers are implemented):
# jina-embeddings        # Layer 2: Semantic search for word-photos
# aiosqlite              # Layer 1: Async SQLite access
