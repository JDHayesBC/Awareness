# Simple Discord Bot Requirements
# Install with: pip install -r requirements.txt

# Discord library - handles all Discord API interactions
discord.py>=2.3.0

# SQLite async support - for message logging (optional but recommended)
aiosqlite>=0.19.0

# Environment variable management - keeps secrets out of code
python-dotenv>=1.0.0
